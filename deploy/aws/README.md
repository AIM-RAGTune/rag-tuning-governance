# AWS ECS/Fargate And Batch

These examples show how to run RAGTune as a finite ECS/Fargate task or AWS Batch job. Replace placeholder account, region, image, role, subnet, and security group values before use.

The examples do not contain AWS credentials and are not AWS-native benchmark results.
