# GitHub Actions Examples

These example workflows build the Docker image and run publication-safe checks. Copy them into `.github/workflows/` in an operator repository if desired.

The public repository keeps them under `deploy/github-actions/` so they are examples, not additional required CI configuration.
