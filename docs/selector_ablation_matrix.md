# Selector Ablation Matrix

The selector ablation matrix compares naive selectors with governed and risk-guarded selectors using sanitized summary artifacts.

The main claim is limited: RAGTune governance blocked selectors that reduced cost or latency at unacceptable quality risk. The artifact does not claim RAGTune always beats other selectors.
