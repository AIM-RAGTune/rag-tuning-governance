# AIM Hardware Matrix

The AIM hardware matrix records sanitized local reproducibility facts such as public node label, machine role, OS family, Python version, CPU architecture, coarse runtime measurements, and artifact sizes.

It does not collect hostnames, usernames, IP addresses, MAC addresses, serial numbers, full environment variables, private paths, or secrets. It is not official platform benchmarking.
