# Artifacts

This directory contains selected manifests, audit outputs, certificates, and small run summaries. Raw datasets, raw CRAG question text, raw source passages, raw API responses, and large raw logs are not included. Selected CRAG artifacts preserve scientific metrics and replace raw wording with hashes and sanitized labels.
