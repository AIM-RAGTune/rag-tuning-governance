# CRAG Mock API Executive Case Pack

## crag_mock_api_case_01

Governance selected `top_k_low` over quality-only's `greedy_regression_aware_search` for `movie` / `multi-hop` because the held-out utility favored the lower-budget/lower-latency choice.

## crag_mock_api_case_02

Governance selected `top_k_low` over quality-only's `greedy_regression_aware_search` for `movie` / `simple` because the held-out utility favored the lower-budget/lower-latency choice.

## crag_mock_api_case_03

Governance selected `top_k_low` over quality-only's `greedy_regression_aware_search` for `movie` / `simple` because the held-out utility favored the lower-budget/lower-latency choice.

## crag_mock_api_case_04

Governance selected `top_k_low` over quality-only's `greedy_regression_aware_search` for `movie` / `simple` because the held-out utility favored the lower-budget/lower-latency choice.

## crag_mock_api_case_05

Governance selected `top_k_low` over quality-only's `greedy_regression_aware_search` for `movie` / `simple` because the held-out utility favored the lower-budget/lower-latency choice.
