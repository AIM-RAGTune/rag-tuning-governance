# Behavioral Distinction Report

Result: `BEHAVIORALLY_DISTINCT_POLICY_TEST_PASSED`.

The matrix compares endpoint sets, API-call counts, context counts, measured cost, latency, abstention, and final quality across frozen CRAG mock-API policy observations. This report contains endpoint identifiers and metrics only; it does not contain raw CRAG query text or raw API responses.
