suite: ragtune_certificate_calibration_v1
seed: 12345
dataset:
  name: fixture_certificate_calibration
policy_space:
  top_k: [3]
objectives:
  cost_weight: 0.25
  latency_weight: 0.1
  protected_regression_threshold: -0.03
baselines:
  - null_policy_a
  - null_policy_b
certificate:
  bootstrap_samples: 100
output:
  append_only: true

