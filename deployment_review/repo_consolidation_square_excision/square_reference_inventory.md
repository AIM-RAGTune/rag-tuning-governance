# Removed Legacy Simulation Reference Inventory

Initial scans found code dependencies, test dependencies, historical mentions, and lineage wording for the removed legacy simulation package.

- Code dependencies: replaced with `ragtune.utils` helpers.
- Test dependencies: removed where they covered only the excluded legacy package.
- Historical mentions: reworded to conservative claim-boundary language.
- Final tracked-content grep for removed package tokens: no hits.
- Filename exceptions: the requested deployment-review directory and inventory filename.
