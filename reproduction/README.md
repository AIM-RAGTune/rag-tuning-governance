# Reproduction

Use the scripts under `scripts/` and the Docker instructions under `reproduction/docker/`.

Some reproductions require raw datasets obtained from original providers. The repository includes processed summaries and selected manifests, not raw licensed data.
