# Reproduce Dataset Matrix

Dataset matrix reproduction requires locally acquired datasets and license approvals.

```bash
make reproduce-dataset-matrix
```

If raw data are missing, the script should fail clearly and point reviewers to `data/DATA_AVAILABILITY.md`.
