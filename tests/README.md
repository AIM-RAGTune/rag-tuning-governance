# Tests

`tests/publication/` contains the default publication-bundle checks that run in CI.

The remaining copied tests preserve the source validation test history for reviewer inspection. Running the complete historical suite may require the original source environment, optional parquet dependencies, mounted dataset paths, and licensed raw data that are not included in this publication repository.
