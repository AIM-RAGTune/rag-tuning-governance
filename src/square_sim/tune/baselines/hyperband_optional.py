from __future__ import annotations


def availability() -> dict[str, str]:
    return {"status": "skipped", "reason": "hyperband is represented by the deterministic successive-subset fallback unless an implementation is configured."}

